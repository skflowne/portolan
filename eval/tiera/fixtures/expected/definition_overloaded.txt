definition for overloaded — src/definitions.ts [0:0-0:50]

```
export function overloaded(value: string): string;
```

definition for overloaded — src/definitions.ts [1:0-1:50]

```
export function overloaded(value: number): number;
```

definition for overloaded — src/definitions.ts [2:0-4:1]

```
export function overloaded(value: string | number): string | number {
  return value;
}
```

3 definitions; complete
